<h4>Create a League</h4>
<form method="post">
    <div class="row">
        <dt>Year:</dt>
        <dd>
            <input id="league-year" type="text" name="year" value="<?php echo $this->escape(date('Y')); ?>"/>
            <input id="league-season" type="hidden" value="<?php echo $this->escape($this->season);?>"/>
        </dd> 
    </div>
    <div class="row">
        <dt>Day:</dt>
        <dd>
            <select name="day" id="league-day">
                <?php foreach(array('Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday') as $day): ?>
                <option value="<?php echo $this->escape($day);?>"><?php echo $this->escape($day);?></option>
                <?php endforeach; ?>
            </select>
        </dd> 
    </div>
    <div class="row submit">
        <input class="button" id="add-league-submit" type="submit" value="Create League" />
    </div>
    <div id="error-string"></div>
</form>